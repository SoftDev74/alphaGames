# alphaGames

You can use expo go app to try this on android. Just run `npm install`, then `npx expo start` (but I've used `npx expo start -c --tunnel`) and then run on android device (connected with USB and "USB Debugging" enabled or by using the expo URL within the expo go app).
